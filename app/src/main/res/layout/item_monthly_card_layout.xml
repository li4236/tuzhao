<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="133dp"
    android:layout_marginEnd="22dp"
    android:layout_marginStart="22dp"
    android:layout_marginTop="22dp">

    <ImageView
        android:id="@+id/monthly_card_iv"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/picture_description"
        android:scaleType="centerCrop"
        app:layout_constraintCircleRadius="10dp" />

    <TextView
        android:id="@+id/monthly_card_type"
        style="@style/TwentyTextB1Style"
        android:layout_marginStart="10dp"
        android:layout_marginTop="12dp"
        android:text="途找停车月卡"
        app:layout_constraintStart_toStartOf="@id/monthly_card_iv"
        app:layout_constraintTop_toTopOf="@+id/monthly_card_iv" />

    <TextView
        android:id="@+id/monthly_card_status"
        style="@style/TwentyTextB1Style"
        android:layout_marginEnd="10dp"
        android:text="生效中"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/monthly_card_type" />

    <TextView
        android:id="@+id/monthly_card_area"
        style="@style/TwentyTextB1Style"
        android:layout_marginBottom="12dp"
        android:text="中山卡"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="@id/monthly_card_type" />

    <TextView
        android:id="@+id/monthly_card_expried_date"
        style="@style/TwentyTextB1Style"
        app:layout_constraintBottom_toBottomOf="@+id/monthly_card_area"
        app:layout_constraintEnd_toEndOf="@id/monthly_card_status" />

</android.support.constraint.ConstraintLayout>